/**
 * Backend URL when running Little Terrarium in a development environment.
 */
export const baseUrlDevelopment = 'http://localhost:5015';

/**
 * Backend URL when running Little Terrarium in a production environment.
 */
export const baseUrlProduction = 'https://littleterrarium.one';

/**
 * Endpoint to be attached to the backend URL, i.e. https://backend.url/v1/api
 */
export const endpoint = '/api';

/**
 * Default theme when first accesed.
 */
export const theme = 'light';

/**
 * List of available themes.
 */
export const availableThemes = ['light', 'dark'];
